# Copy to ".env" in this folder (same directory as main.py) and fill in values.
# The ".env" file is gitignored — never commit real keys.
#
# Google AI Studio (free tier for development): https://aistudio.google.com/apikey

GEMINI_API_KEY=

# Optional: Godot project root when you run `python main.py` without --project-root
# GODOTTER_PROJECT_ROOT=C:/Projects/YourGame
